# ─────────────────────────────────────────────────────────────────────────────
# Al-Ruya ERP — Production environment template
# Copy to .env on the VPS, fill in real secrets, NEVER commit the filled file.
# Generate strong secrets with:  openssl rand -base64 32
# ─────────────────────────────────────────────────────────────────────────────

# ── Compose project ─────────────────────────────────────────────────────────
# Pin the Compose project name so volumes (e.g. infra_postgres-data) are
# always scoped consistently. Without this, Compose derives the project name
# from the working directory, which means renaming /opt/<dir> on the VPS
# would silently create a NEW set of volumes and break recovery. Pinned name
# also prevents accidental collisions if a sibling app is ever deployed on
# the same host.
COMPOSE_PROJECT_NAME=al-ruya-erp

# ── PostgreSQL ──────────────────────────────────────────────────────────────
POSTGRES_PASSWORD=CHANGE_ME_strong_random_password

# ── Redis ───────────────────────────────────────────────────────────────────
REDIS_PASSWORD=CHANGE_ME_strong_random_password

# ── JWT (auth) ──────────────────────────────────────────────────────────────
JWT_SECRET=CHANGE_ME_at_least_32_random_chars
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=30d

# ── System owner (permanent account) ────────────────────────────────────────
# Set these BEFORE running seed. They are read by prisma/seed-bootstrap.ts
# and are NEVER committed to source. Singleton — only one user can own.
# Username can be a handle (no spaces), password must be strong.
OWNER_USERNAME=
OWNER_PASSWORD=
OWNER_EMAIL=

# ── Test admin (optional — for non-2FA testing) ─────────────────────────────
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=CHANGE_ME_strong_random_password

# ── MinIO (S3 object storage) ───────────────────────────────────────────────
MINIO_ACCESS_KEY=alruya_admin
MINIO_SECRET_KEY=CHANGE_ME_strong_random_password

# ── App URL (used for CORS, links in emails, etc.) ──────────────────────────
# First deploy: use VPS IP. Switch to https://erp.your-domain.com once SSL is ready.
APP_URL=http://187.124.183.140
CORS_ORIGINS=http://187.124.183.140

# ── Optional: AI Brain (only if running --profile ai) ──────────────────────
AI_BRAIN_API_KEY=
AI_VERSION=latest

# ── Optional: WhatsApp Bridge (only if running --profile whatsapp) ─────────
WHATSAPP_BRIDGE_TOKEN=
WA_VERSION=latest

# ── Optional: Backups (Restic) ──────────────────────────────────────────────
RESTIC_REPOSITORY=/backups/restic-repo
RESTIC_PASSWORD=CHANGE_ME_strong_random_password

# ── Optional: Grafana (only if running --profile monitoring) ───────────────
GRAFANA_ADMIN_PASSWORD=CHANGE_ME

# ── Optional: License server (Wave 6) ───────────────────────────────────────
LICENSE_ADMIN_TOKEN=
